# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Secrets / config
.env
.env.*
!.env.example
*.pem
*.key
.hubspot_mcp_tokens.json

# Runtime output — never committed
logs/
# Run artifacts: drafts and per-run transcripts. These DO carry a lead's name
# and email address, so they never leave the machine that produced them.
artifacts/
*.log

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# stale files this session could not delete (remove the folder yourself)
_to_delete/
