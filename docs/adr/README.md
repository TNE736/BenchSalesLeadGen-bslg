# Architecture decision records

One file per decision, numbered: `001-short-slug.md`. Once accepted, an ADR is
immutable — supersede it with a new one rather than editing it.

Write an ADR when: a choice constrains later steps, a design-document assumption is
overridden, or anything is reused from LQABR.
